#!/usr/bin/env groovy
/*
 * Set up the basic levels of authorization and other security settings which
 * should be defaulted in 2.0, but are not in our instances because we're
 * skipping the setup wizard
 */
