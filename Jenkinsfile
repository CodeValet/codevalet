#!/usr/bin/env groovy

pipeline {
    agent { label 'linux' }
    stages {
        stage('Build') {
            steps {
                echo 'Nothing to do here just yet'
            }
        }
        stage('Test') {
            steps {
                echo 'Maybe rtyler will write some tests'
            }
        }
    }
}
